// This is a normal entry, for developing without system.js
import React from "react"
import ReactDOM from "react-dom"
import { AppDev as RootComponent } from "./routes/app"

ReactDOM.render(<RootComponent />, document.getElementById("app-react"))
